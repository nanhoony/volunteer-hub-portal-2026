# 2025 자원봉사 모집공고 데이터 처리 및 웹 포털 구축 과제 제출

## 1. 사용 AI 도구
- **Google Antigravity IDE (Gemini 3.7 Flash Agent)**: Python 자동화 데이터 분리 및 집계 스크립트 작성, 모던 반응형 정적 웹 애플리케이션 설계 및 무료 호스팅 배포 지원

## 2. 구현 메모
- **파이썬 자동화 스크립트 (`solution.py`)**: `csv`, `os`, `sys`, `collections` 등 Python 표준 라이브러리만을 활용하여 `모집공고.csv`의 인코딩(UTF-8-SIG/CP949)을 안전하게 처리하고, 7개 분야별 개별 CSV를 `출력_분야별/` 디렉토리에 자동 분리 저장한 후 `결과_분야집계.csv` 생성 및 콘솔 요약 출력을 완료했습니다.
- **정적 웹 포털 UI (`index.html`)**: 분야별 핵심 지표 요약 카드, 실시간 키워드/지역/상태 다중 필터링 및 '결과 N건' 동적 표시를 구현했으며, `카드뉴스 1~5.png` 이미지를 활용한 라이트박스 확대 및 반응형 카드 레이아웃을 지원합니다.
- **무료 정적 호스팅 배포**: Surge 공식 계정(`nanhoony@gmail.com`)으로 인증 및 배포를 완료하여 권한 제약 없이 24/7 가동되는 Public 오픈 URL(`https://nanhoony-volunteer-hub-2025.surge.sh` 및 `https://nanhoony-volunteer-2026.surge.sh`)을 배포하고 `배포_URL.txt`에 저장했습니다. 백그라운드 Keep-Alive 데몬을 통해 CDN 웜업 및 무중단 고가용성 운영을 유지합니다.
